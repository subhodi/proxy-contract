## Ethereum proxy contract and example

### Setup
```bash
$ git clone https://github.com/subhodi/proxy-contract.git
$ npm install -g truffle
$ npm install -g ethereumjs-testrpc
$ testrpc
$ truffle compile
$ truflle migrate
$ truffle test
```